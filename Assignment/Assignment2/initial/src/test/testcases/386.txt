 Function: hmmOkmsa
        Body:
                Do
                    For (i = 0, i < smaller, 1) Do
                    sum = sum + arrA[i] + arrB[i];
                EndFor.
                    If diffLen > 0 Then 
                    isA = True; 
                    smaller = len(arrA);
                Else 
                    isA = False; 
                    smaller = len(arrB);
                EndIf.
                    Continue;
                While (3-2) == 1+2*test(1) EndDo.
            
        EndBody. 